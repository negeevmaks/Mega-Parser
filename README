# Auto Listings Web Scraping Project

## Project Overview

The Auto Listings Web Scraping Project focuses on extracting car sale listings from different websites to compile information about cars that are currently available for purchase in Ukraine. The script is designed to retrieve essential details including the car's model, price, and the contact details of the seller.

## Requirements

Before embarking on this project, ensure that you have fulfilled the following prerequisites:

- Python 3.x is installed on your system.
- The following Python packages are installed:
  - `requests`
  - `beautifulsoup4` (BeautifulSoup)
  - `openpyxl`

You can effortlessly install the necessary packages by executing the following command:

```bash
pip install requests beautifulsoup4 openpyxl
```
